#include <bits/stdc++.h>
using namespace std;

int32_t main() {
  ios_base::sync_with_stdio(0);
  cin.tie(0);

  int mask = 0; // temporary
  for (int submask = mask; submask; submask = (submask - 1) & mask) {
    
  }

  return 0;
}